# DualCode Workbench 项目状态

## 2026-07-19 Phase T1 Claude thinking 块接入

- `ClaudeStreamParser` 已把 Claude `thinking` 内容映射为现有 `delta + reasoning item`
  活动事件，直接复用 A2 的「正在思考」与可折叠思考过程 UI；普通 `text` 仍走正文增量。
- `redacted_thinking` 只生成不含协议数据的终端诊断，不进入思考正文，避免 signature、
  data 等脱敏内容泄漏；本地 Claude CLI 与 VPS SSH 两条路径继续共用同一解析器。
- 新增协议及双路径回归测试；验证通过：后端 116 项、前端 75 项、TypeScript 与 Ruff。
  按执行约定停在 T1，真实 VPS 展示效果留待安装包验收，不进入 T2。

## 2026-07-19 协作闭环方向确认与规格清单

- 用户确认当前工具「离真正可用有差距」的两个核心问题：思考过程未先行流式展示；
  Codex/Claude 之间没有自动接力，用户被迫充当信使推动每一环交接。
- 协作形态选定「自动接力循环」（Codex 实现 → Claude 审查 → 发现自动回注修复），
  Claude 保持在 VPS 运行是硬约束，同步机制以 Git 影子 ref 自动推送替代手动 commit/push。
- 思考缺失根因已在代码定位：`claude_stream.py` 丢弃 `thinking` 块；Claude CLI 调用缺
  `--include-partial-messages` 导致无逐 token 流式；Codex turn 配置疑似未开启 reasoning summary。
- 新增 `docs/RELAY_LOOP_BACKLOG.md`：Phase T（思考可见性 T1-T3）与 Phase R0-R3
  （影子同步、审查协议、编排状态机、上下文与 UI），含保护条款与安全不变量修订说明。
  执行约定沿用 REMEDIATION_BACKLOG，Codex 按序执行、Claude 逐 Phase review。

## 2026-07-14 Claude App 对齐 A7

- U5 主题层迁移为暖中性配色：`surface-0 #1f1e1b`、`surface-1 #262521`、`surface-2 #302e29`、
  正文 `#f5f4ef`、次级文本 `#cbc5bb`、弱文本 `#a49e94`、陶土强调色 `#d97757`。
- 主操作及其 hover/pressed 状态统一使用 `#d97757 / #e18666 / #c86648`，按钮文字使用高对比深暖色 `#211b18`；
  Codex/Claude 仅名字分别使用低饱和 `#d6a57f / #b8a6c7`，不恢复头像或左边框。
- 代码块、工具原始输入和终端显式锁定为中性深灰 `#111315`，不随主题暖化；规则、交接与恢复页同步接入主题 token。
- 实际浏览器渲染检查修正了创建/克隆、VPS 保存检测和测试配置三个残留蓝色主按钮；对比度策略测试覆盖正文、弱文本和主按钮文字。
- 验证通过：前端 75 项、TypeScript、严格 ESLint、A7 改动文件 Prettier、Ruff、后端 113 项全量测试、
  Vite 生产构建、Windows sidecar 与 Tauri release `--no-bundle` 构建。按约定暂停，等待用户截图确认。

## 2026-07-14 Claude App 对齐 A8

- 新任务空状态由功能说明卡收敛为居中大号问候语和一行轻提示，不再展示机器人图标或技术流程说明。
- system 事件移除绿色状态边框、底色和 `System` 标题，改为对话内容轨道内的低对比度行内灰字；时间仍保留在右侧。
- 验证通过：前端 74 项、TypeScript、严格 ESLint、A8 改动文件 Prettier、Ruff、后端 113 项全量测试、
  Vite 生产构建、Windows sidecar 与 Tauri release `--no-bundle` 构建。

## 2026-07-14 Agent 消息复制入口精简

- 根据安装包验收反馈，移除 Codex/Claude 消息右上角的整条消息复制工具条，避免遮挡正文和形成不必要的悬浮控件。
- 用户消息的“编辑后重新发送/重试本轮”工具条继续保留；代码块级复制继续保留，便于精确复制代码。
- 验证：前端 60 项、TypeScript 与严格 ESLint 全部通过。

## 2026-07-14 U3 流式渲染策略

- `stream-*` 占位消息在生成期间只渲染 `pre-wrap` 纯文本与尾部光标，不再随每个 delta 重跑 Markdown 解析和重排。
- `message.created` 把占位 ID 替换为持久消息 ID 后，`MessageCard` 自动恢复 Markdown 渲染，不引入额外切换状态。
- 未修改消息跟随逻辑；既有“接近底部才跟随、用户上翻不强制吸底”回归继续通过。
- 验证：前端 57 项、TypeScript、严格 ESLint和生产构建通过，主 JS 543.58kB 且无新增 chunk 警告。后端未修改；本机 pytest 仍因已确认的 Windows 临时目录 ACL 问题得到 62 项通过、41 项环境错误，U2 review 已在 Linux 与 CI 补验后端 103 项全绿。

## 2026-07-14 U2 消息工具条与复制

- Codex/Claude 消息新增右上角 hover/focus 工具条，可一键复制完整 Markdown 原文；用户消息的“编辑后重新发送”和“重试本轮”收敛到同一交互样式，原行为保持不变。
- 工具条按钮支持键盘聚焦；无 hover 的触屏环境直接保持可见，避免只靠鼠标悬浮才能操作。
- U1/U2 共同复用 `useCopyFeedback`：剪贴板写入成功显示“已复制”1.5 秒，权限拒绝显示“复制失败”，不再产生未处理 Promise rejection。
- 验证：前端 56 项、TypeScript 与严格 ESLint 通过。后端代码未修改；后端全量测试因当前 Windows 测试临时目录 ACL 拒绝创建子目录而无法完成，首次收集 4 项错误，沙箱外重跑为 62 项通过、41 项环境错误。

## 2026-07-14 U1 代码块阅读与操作体验

- Markdown 块级代码由独立 `CodeBlock` 接管，highlight.js 通过 core 按需注册 TypeScript、JavaScript、Python、Rust、Bash、JSON、Diff、CSS、HTML/XML、SQL、YAML 与 Markdown，不识别的语言安全回退纯文本。
- 代码块提供语言标签、hover/focus 可见的复制按钮与 1.5 秒就地成功反馈；超过 400 行默认展示前 80 行，可显式展开全文。
- 验证：前端 53 项、TypeScript、严格 ESLint、U1 改动文件 Prettier 与生产构建通过；主 JS 542.51kB，无新增 chunk 警告。

## 2026-07-14 对话消息垂直节奏修复

- 用户、Codex 与 Claude 消息统一使用 24px 轮次间距；用户消息的编辑/重试操作栏改为绝对定位，不再因透明但占位造成相邻消息间距忽大忽小。
- Agent 正文增加明确的上下内边距，避免首行紧贴标题分隔位置、末行紧贴后续消息或输入区；新增样式策略回归测试。
- 验证：前端 50 项、后端 103 项、TypeScript 类型检查与严格 ESLint 全部通过。

## 2026-07-13 Phase 3 后端结构与可维护性

- Phase 3 前置 R5 已完成：`api.py` 全部用户可见 HTTP 错误中文化，动态技术错误增加中文外层，高频项目、任务、审批和附件路径补充中文断言；Ruff 与后端 95 项通过。
- Phase 3 前置 R6 已完成：InputDialog 增加输入法组合态守卫，避免候选确认时误提交，并补组件回归测试；TypeScript、严格 ESLint、Prettier 与前端 33 项通过。Phase 3 已解锁。
- P3-1 已完成：Alembic 接管数据库 schema，新增冻结 baseline 与旧手写补丁兼容 revision；全新、补丁前和补丁后数据库均可保留数据升级。后端 98 项、Ruff、sidecar 构建及正式 windowed 产物空库迁移启动通过。
- P3-2 已完成：原 1465 行单体 API 按执行任务、Agent 设置、协作治理、工作区会话和附件五个领域拆为独立 router，公共运行态与交接提示词独立封装；98 项后端测试、Ruff、桌面端类型检查及路由重复检查通过。
- P3-3 已完成：Codex app-server 与 Claude stream-json 的协议解析下沉到各自 adapter，对调度器统一输出 `delta / tool_event / terminal / final` 事件；scheduler 仅做事件转发和结果聚合，协议与韧性测试迁移后后端 100 项通过。
- P3-4 已完成：对话上下文改为按 60k 字符从新到旧流式装载，项目与任务契约使用独立 20k 字符预算；两者超限均插入明确截断标记，避免固定消息条数和无界契约挤占上下文。
- P3-5 已完成：前端 store 新增 WebSocket 核心归并测试，覆盖 `agent.delta` 流式拼接、`message.created` 替换占位、`agent.tool` 活动时间线、错误终止运行态和断线重连；前端 37 项测试通过。Phase 3 全部完成，等待 Review 3。
- 安装验收修复：VPS 仓库已经由实时检测确认就绪时，界面不再被历史 `FAILED` 克隆任务覆盖；空仓库会正确显示“已就绪，等待首次提交”。前端 38 项测试通过，修复版提升为 `0.1.6` 以避免与旧安装包混淆。

## 2026-07-13 Phase 2 前端设计与信息架构

- P2-1 已完成：建立统一字号、弱文本与键盘焦点 token，移除小于 11px 的文本和裸焦点轮廓；TypeScript、前端 16 项、严格 ESLint 与 Prettier 通过。
- P2-2 已完成：手写消息格式化器替换为禁用 raw HTML 的 `react-markdown + remark-gfm`，支持标题、链接、列表、代码块和表格；TypeScript、前端 18 项、严格 ESLint 与 Prettier 通过。
- P2-3 已完成：真实 Git Diff 按文件拆分导航，Monaco 改为进入变更页后动态加载；初始 JS 降至 459kB，生产构建无 chunk 警告，TypeScript、前端 20 项、严格 ESLint 与 Prettier 通过。
- P2-4 已完成：交接预览移除裸 JSON，按契约摘要、仓库基线、修改文件、可折叠 Diff 和测试证据结构化展示，并提供中文状态与文件/增删行统计；TypeScript、前端 20 项、严格 ESLint 与 Prettier 通过。
- P2-5 已完成：单条全局错误升级为右下角分级通知队列，错误持久保留、后台信息 5 秒自动消失并支持逐条关闭；TypeScript、前端 22 项、严格 ESLint 与 Prettier 通过。
- P2-6 已完成：首条消息自动生成任务标题，侧栏支持双击/菜单行内重命名和两步确认删除；后端持久化、运行态保护、级联清理及审计闭环完成。后端 94 项、前端 24 项及全部静态门禁通过。
- P2-7 已完成：底部后台任务可跳转到对应项目/任务并标注快照状态语义，项目搜索无结果时显示明确空状态；TypeScript、前端 25 项及静态门禁通过。
- P2-8 已完成：设置与项目对话框统一支持初始聚焦、Esc 和 Tab 焦点圈定；全部浏览器原生 confirm/prompt 已替换为应用内确认/输入对话框，完全访问文案平台中性化。前端 26 项及静态门禁通过。
- P2-9 已完成：Composer 支持 40vh 内自动增高、单一附件入口、附件超限通知与拖放高亮；图片灯箱支持非 passive 滚轮缩放和指针拖拽平移。TypeScript、前端 29 项与严格 ESLint 通过。
- P2-10 已完成：VPS 仓库配置和状态收敛为单一独立组件，作业反馈改由纯函数派生，删除重复旧操作并安全处理空 HEAD；克隆运行、失败修复重试和就绪三态已有组件测试。TypeScript、前端 32 项与严格 ESLint 通过。
- P2-11 已完成：Agent、后台服务、CLI、SSH、Codex app-server 与 HTTP 错误统一增加中文用户语义，同时保留路径、退出码和原始错误等技术细节。后端 94 项、前端 32 项、TypeScript 与严格 ESLint 通过。
- Phase 2 最终验收完成：R3、R4、P2-1 至 P2-11 全部关闭；后端 94 项、前端 32 项、TypeScript、严格 ESLint、Ruff、Prettier、Vite 生产构建、Windows sidecar 与 Tauri release `--no-bundle` 构建通过。按执行约定停止，等待 Claude review，不进入下一阶段。

## 2026-07-13 Phase 1 前端高严重度交互整改

- P1-1 已完成：Composer 在中文输入法候选态忽略 Enter，避免误发送；前端类型检查与 8 项组件测试通过。
- P1-2 已完成：契约列表与测试参数 textarea 改为原始字符串编辑态，保存时再过滤空行；前端类型检查与 9 项组件测试通过。
- P1-3 已完成：待审批操作移入中央对话流，不受检查器隐藏或窄屏布局影响；前端类型检查与 10 项组件测试通过。
- P1-4 已完成：线程 WebSocket 支持指数退避重连、坏帧保护、重连补偿刷新与可见连接状态；前端类型检查与 11 项测试通过。
- P1-5 已完成：消息流仅在接近底部时自动跟随，上翻后显示“回到最新”且不强制吸底；前端类型检查与 12 项测试通过。
- P1-6 已完成：实现真实 Ctrl/Cmd+O 打开项目快捷键，项目菜单支持外部点击与 Esc 收起；前端类型检查与 13 项测试通过。
- P1-7 已完成：ProcessingCard 使用运行事件固化的 Agent 身份，发送目标变化不会改名；前端类型检查与 14 项测试通过。
- Phase 1 最终验收完成：P1-1 至 P1-7 全部关闭；后端 90 项、前端 14 项、TypeScript、Prettier 与 ESLint（0 error，保留 5 条既有 warning）通过。按执行约定停止，等待 Claude review，不进入 Phase 2。
- Phase 2 前置 R3 已完成：WebSocket 鉴权覆盖缺失/错误 token 的 4401 拒绝及正确 token 建连事件；专项 5 项、后端全量 93 项通过。
- Phase 2 前置 R4 已完成：ESLint 既有 5 条 warning 全部清零，lint 脚本收紧为 `--max-warnings=0`；TypeScript、前端 14 项、严格 ESLint 与 Prettier 全部通过，Phase 2 已解锁。

## 2026-07-13 Phase 0 产品化整改

- P0-1 已完成：消息执行模式只允许 Codex/Claude，旧自动编排流水线、Mock Diff 和伪造测试记录路径已删除；后端全量 68 项通过。
- P0-2 已完成：删除前端演示夹具、sample 魔法路径和离线伪造回复；后端离线时编辑器与附件操作明确禁用，并提供中文重试入口；前端 7 项测试与类型检查通过。
- P0-3 已完成：E2E 改为真实空状态与设置对话框冒烟，不依赖演示数据或后端；Playwright 1 项通过，并补充 README 一键命令。
- P0-4 已完成：建立 Windows/Linux CI，覆盖 Ruff、pytest、ESLint、类型检查和 Vitest；前端源码已独立完成 Prettier 格式化。本地 Ruff、后端 68 项、前端 7 项、lint、格式及类型检查通过。
- P0-5 已完成：Tauri 每次启动生成独立 sidecar token，FastAPI 统一保护全部 `/api/*` HTTP 与 WebSocket，前端请求、附件和实时连接统一携带鉴权信息；浏览器开发模式支持用户数据目录 token 文件。后端 70 项、前端 7 项、类型检查、lint 与 Rust `cargo check` 通过。
- P0-6 已完成：凭据防护集中为带原因说明的 glob 规则列表，覆盖环境文件、证书/私钥、SSH 身份文件、常见凭据配置与密钥库；专项 22 项、后端全量 90 项和 Ruff 检查通过。
- Phase 0 最终验收完成：后端 90 项、前端 7 项、E2E 1 项、Ruff、TypeScript、ESLint（0 error，保留 5 条既有 warning）、Prettier 与 Rust `cargo check` 全部通过。按执行约定停止，等待 Claude review，不进入 Phase 1。
- Phase 0 Claude review 返工已完成：CI 在 `setup-node` 前安装固定 pnpm 并指定 pip 缓存依赖文件，后端正式声明 Pillow；全新虚拟环境确认自动安装 Pillow 11.3.0，本地等价 CI 全绿，等待 GitHub Actions 双平台最终确认。
- CI 首次返工复验进一步发现 pnpm 11 的依赖构建审核占位值；已仅允许前端工具链必需的 esbuild 构建脚本，保持其他依赖默认拒绝，等待双平台再次确认。
- Phase 0 返工最终验收完成：GitHub Actions CI #4（run `29244288260`）在 Ubuntu 与 Windows 两个平台全部成功，Claude review 的 R1、R2 已关闭。

## 2026-07-13 VPS 克隆显式授权

- 用户点击“克隆到 VPS”即构成本次克隆的明确授权，不再创建需要二次确认的待审批弹窗。
- 克隆仍持久化 APPROVED 授权、ExecutionJob、证据和审计日志，支持失败展示、重启恢复与显式重试。
- 自动后台克隆仍被禁止；对已有目录的覆盖、拉取、推送、删除等高风险操作没有放宽审批。
- 克隆按钮显示“正在克隆”，提交后明确提示已开始执行并会自动刷新仓库状态。
- 验证：执行作业专项测试 10 项、后端全量测试 64 项、前端组件测试 4 项与 TypeScript 类型检查全部通过。
- 已重新生成 sidecar，并完成 Tauri release `--no-bundle` 构建；最新可执行文件位于 `apps/desktop/src-tauri/target/release/dualcode-workbench.exe`。
- 修复旧版本 `remote_git_provision` 待审批遗留：启动恢复时自动取消冗余审批，用户再次点击克隆时也会原子替换旧记录，不再被“请先处理当前待审批操作”阻塞。
- VPS 项目目录尚未克隆时返回明确的 `not_cloned` 状态，不再把 Git `exit 128` 当作需要展示的检测故障；真实 SSH、鉴权和 Git 错误仍保留完整反馈。
- 本轮验证更新为后端全量 66 项、前端组件 4 项和 TypeScript 类型检查全部通过。
- VPS 仓库就绪检测覆盖半初始化目录：工作树、有效 HEAD 或 origin 任一缺失均返回 `not_cloned`，保存配置不再被 Git `exit 128` 误报为失败；配置持久化与仓库可用性检测在交互语义上明确分离。
- VPS 克隆交互改为持久化作业驱动：运行中持续显示进度并禁用重复点击，成功或失败通过 WebSocket/作业刷新回写到仓库卡片，失败展示脱敏后的真实 Git 原因并提供“重新克隆”；后端对同一活动操作幂等复用，不再生成重复作业。

## 2026-07-13 右侧检查器精简

- 右侧一级导航由六个平铺页面精简为“状态、规则、交接”。
- 原“上下文、Diff、终端”合并到“状态”，使用“仓库、变更、运行日志”二级导航。
- “恢复”改为事件入口，仅在存在失败、中断或可撤销运行时出现；问题清除后自动返回状态页。
- 审批保持事件驱动，仅在当前轮次等待授权时显示审批卡。
- 前端 4 项组件测试、后端 59 项测试通过。

## 2026-07-13 Claude 远端目录职责修正

- 全局“远端临时目录”改为可选的“远端运行根目录”，只保存任务隔离运行数据和显式附件，不再与项目 VPS 仓库路径混用。
- 留空时按 SSH 用户自动解析为 `~/.dualcode`；项目源码仍只在项目级 VPS 仓库路径中读取。
- 旧版共享目录 `/tmp/dualcode-workbench` 加载时自动迁移为用户隔离默认值。
- 设置界面移除未接入执行链路的“本地 Claude CLI”，并明确不会完整上传本地项目。
- Claude 设置新增全局“VPS 项目根目录”；项目仓库路径按远程仓库名自动派生，项目状态页不再要求手工输入 VPS 路径。
- VPS 仓库保存、准备、获取和拉取操作新增明确的加载、成功与失败反馈，操作期间禁用重复点击，不再出现点击按钮无响应。
- VPS clone 审批文案与动作一致；审批通过后推送执行中状态，完成后自动刷新仓库；远端 Git 失败展示真实 stderr，“准备 VPS 仓库”改名为“克隆到 VPS”。

更新时间：2026-07-12

## 当前阶段

项目进入“专业产品化基线与发布验收”阶段。桌面端、冻结后端、真实 Agent 适配器、持久化执行作业和统一发布门禁已经可运行；正式安装包与全量真实故障场景仍待验收。

## 已完成

- Tauri 2 + React + TypeScript + Vite + Zustand + Tailwind 风格桌面界面。
- 可拉伸、隐藏的左右面板，自定义窗口标题栏和深色开发工具主题。
- FastAPI + SQLite + SQLAlchemy，本地 sidecar 随桌面应用启动和回收。
- Workspace / Thread / Message / AgentRun / Attachment / FileChange / TestRun / Approval / Audit 数据层。
- WebSocket 消息、状态、终端、工具事件和测试结果推送。
- Codex 与 Claude 独立多轮会话；不是固定自动工作流。
- 本地 Codex CLI、VPS Claude SSH/SFTP 真实适配器及 MockAdapter。
- Codex/Claude 模型和推理强度设置，Claude 默认 Opus 4.8 展示选项。
- 图片选择、拖放、剪贴板粘贴、类型/大小校验、图片重编码清理元数据。
- Git 状态、Diff、提交、推送、fast-forward 拉取和审批。
- VPS 仓库 URL/路径配置、状态对比、fetch/pull 和审批。
- 独立测试执行器、实时输出、结果持久化和审计。
- Windows 后台子进程无控制台窗口。
- 新建任务互斥与空草稿复用，避免重复空任务。
- Agent 工具事件按运行聚合，并固定显示在该轮最终回复之前；真实进程输出进入终端页。
- 全新数据库不再自动注入演示工作区和任务；仅保留早期乱码标签兼容修复。
- 桌面端初始 Zustand 状态已改为空列表，不再在后端加载完成前短暂渲染前端演示工作区。
- 新增完整项目接入入口：可对空目录执行 `git init -b main`、可选关联 origin，或从远程 URL clone；操作前明确展示计划且不会自动提交或推送。
- 项目菜单支持“从工作台移除”，会阻止移除仍有运行任务的项目，仅清理工作台数据库记录和远程配置，不删除本地仓库目录。
- 修复桌面 WebView 删除请求的 CORS 预检：后端显式允许 `DELETE`，不再在移除成功前被浏览器拦截为 `Failed to fetch`。
- Git 状态支持尚无首次提交的 unborn branch，新建空仓库可直接进入工作台继续由 Codex 创建项目。
- 克隆空远程仓库后会明确显示“远程仓库为空”和首次开发引导，区分“已关联 origin、等待首次推送”与克隆失败。
- 创建新仓库或克隆空远程时会在项目接入阶段生成技术栈无关的 README 与首次本地基线提交；不会自动推送，进入对话后从产品目标、场景和验收标准开始澄清需求。
- 项目接入接口按规范化本地路径幂等，前端同步防止双击重复提交；首次请求成功后不会因第二次请求看到非空目录而误报失败。
- 对话流新增持续可见的 Agent 处理卡：区分“正在思考”“正在处理”“运行测试”和“等待授权”，避免仅靠标题栏状态造成任务卡住的错觉。
- Codex JSONL 过程事件新增流式映射：reasoning 摘要、命令开始/完成、文件变更、MCP 调用和搜索会实时进入活动时间线；隐藏思维链不展示，最终文本按 CLI 提供的完整消息呈现。
- Agent 取消链路改为确定性终止：请求后立即将线程恢复为空闲、活动运行标记取消并推送状态，再等待任务/子进程回收；启动时自动修复崩溃遗留的运行中 Agent 状态。
- 普通 Codex/Claude 编辑授权新增“允许本任务”，仅在当前任务和当前应用会话内复用；Git、删除、安装、联网和测试等高风险操作仍逐次审批。
- 消息区改为紧凑对话布局，用户气泡与 Agent 内容分层展示，并支持段落、列表和 fenced code block 的基础格式化。
- Git、远程 Git 和测试审批操作已持久化为 ExecutionJob，包含不可变参数、幂等键、状态、尝试次数、错误和时间戳。
- 后端启动会恢复待审批和已批准作业；崩溃中的副作用作业标记为 INTERRUPTED，要求显式重试，不自动重放 commit/push。
- Agent 工具事件升级为按 run 归组的步骤时间线，展示可读工具、详情及运行/完成/失败状态；错误与恢复建议进入对应运行卡片。
- 建立统一发布检查脚本与交付清单，覆盖后端测试、前端类型检查、前端测试和补丁完整性。
- 提供线程级执行作业查询、详情和显式重试 API；审批归属、状态迁移和执行认领均使用条件更新，避免并发重复副作用。
- 前端新增作业恢复中心，可查看失败/中断原因、尝试次数并显式重试已审批作业。
- 数据库时间统一为带时区 UTC，兼容读取旧 SQLite naive UTC 数据。
- Git 与远程 Git 作业持久化执行前后证据；启动时仅对 `verified` 结果对账成功，证据不足保持 outcome unknown 且不自动重放。
- Agent 韧性测试覆盖 session 续接、流式解析、无效输出、非零退出、超时清理和显式取消；CLI 健康探测超时会回收子进程。
- Windows 发布布局检查会校验版本、Tauri bundle、sidecar、桌面 EXE 及当前版本 MSI/NSIS；产品版本统一为 0.1.5。
- 新增安全诊断快照 API，覆盖数据库、数据目录、Agent、作业、审计与 sidecar 运行状态，且不返回配置值、敏感路径、异常正文或审计明细。
- 恢复中心展示脱敏的 Git 执行前后证据、核验状态、结果未知风险和安全恢复建议。
- API 集成验收覆盖审批恢复、Git 审计、PNG 附件、Diff、测试详情和双 Agent 独立健康；提供只读 Windows 安装后 smoke 脚本。

## 已验证

- 双 Agent 结构化交接闭环已完成：新增持久化交接包与“交接”页，可从实时契约、Git 基线、修改文件、Diff 和测试证据生成预览；不会复制聊天全文；用户确认后才发送给 Codex 做仓库验证或 Claude 做独立审查。Claude 提示词强制输出未实现、部分实现、潜在问题、回归、临时架构违规和证据缺口；Codex 提示词强制区分仓库事实与建议。交接生成、发送均审计，后端 59 项、前端 4 项和类型检查通过。
- 项目治理第一阶段纵向闭环已完成：新增持久化项目目标/边界/规则/交付物与任务目标/非目标/验收标准/约束/风险/阶段；“契约”页支持编辑并显示实施门禁；产品级“禁止 Demo、临时方案和不可持续架构”原则由后端强制保留；Codex/Claude 每轮自动接收结构化项目与任务契约，并被要求指出未覆盖内容、潜在问题、回归和证据缺口。后端 59 项、前端 4 项测试及类型检查通过。
- Agent 设置新增 Codex 执行权限三档：安全审批（`on-request + workspaceWrite`）、工作区自动开发（`never + workspaceWrite + 默认禁网`）和完全访问（`never + dangerFullAccess`）。完全访问保存前强制风险确认，配置持久化后会重建适配器并用于后续轮次；协议/API 13 项与前端 4 项测试、类型检查通过。
- Codex 三项关键交互缺口完成首轮实现：app-server 改为 `on-request + workspaceWrite + 默认禁网`，原生命令、文件变更和权限扩大请求进入应用持久化审批；Agent 成功/失败/取消/重启中断状态进入恢复中心；用户消息支持编辑后重发与重试，普通输入可继续对话；每轮 Codex 保存前后 Diff 检查点，撤销必须审批且当前 Diff 完全匹配才执行。新增原生审批协议测试，后端 56 项、前端 4 项、类型检查全部通过。
- 聊天图片缩略图已升级为应用内灯箱预览：支持点击居中放大、滚轮及按钮缩放、恢复实际大小、打开原图、点击遮罩和 Esc 关闭；前端类型检查与 4 项组件测试通过。
- 修复用户消息附件不可见：附件在发送时持久绑定到对应消息，工作区历史接口和发送响应返回附件元数据，聊天气泡展示图片缩略图并支持打开原图；Tauri CSP 已允许本地附件内容接口，前端还会用发送响应确认附件归属，避免 WebSocket 时序丢失。旧数据库启动时自动追加兼容字段。后端 56 项、前端 4 项测试及 TypeScript 类型检查通过。

- 修复图片附件落盘路径：经 Pillow 验证/重编码后按 MIME 保存 `.png/.jpg/.webp` 后缀，文本保存 `.txt`，确保 Codex app-server `localImage.path` 可被本地图片加载器识别。

- 修复活动 `<details>` 的非受控 open 状态：终态由组件主动收起；reasoning delta 按 item 累积，完成事件仅更新状态而不再把摘要覆盖为 `reasoning`。

- app-server stderr 不再丢弃：保留最近诊断行并随安全错误返回；stdout 结束会等待进程并清空失效句柄，下轮可重新拉起。未知服务端请求返回明确 JSON-RPC 不支持错误，避免协议悬挂。
- 普通 Codex 对话完成后同步采集真实 Git diff/changed files、替换该任务的 FileChange 快照并推送工作区变化事件；提示词明确禁用尚未接入的 imageGeneration。

- “允许本任务”的普通 Codex/Claude 编辑授权由纯内存缓存升级为审计记录恢复：同一任务在 sidecar 或应用重启后继续复用；删除、安装、联网、测试和 Git 等高风险操作仍逐次审批。

- Agent 活动区按 Codex CLI 信息结构重做：运行时展开，终态默认折叠为“已处理/处理失败 + 耗时”，协议字段不再直接展示，命令与过程仅在展开后查看，失败原因使用独立轻量提示。

- 对话新增消息、流式 delta 和运行状态变化后会在下一帧将消息尾部滚动到输入框上方，并保留底部安全间距，避免最新用户消息或处理状态被输入框遮挡。

- Codex/Claude 正文块左边缘改为与 Agent 图标左边缘对齐，身份标题仍位于图标右侧，形成清晰的一轮回复层级。

- Agent 失败事件现在会原子化终止活动卡内所有仍在运行的步骤，避免整轮已失败但局部步骤继续显示旋转动画；新增纯状态回归测试。

- 真实 app-server 联调已确认可完成回复；消息层修复了协议 `userMessage/agentMessage` 被误记为工具、Codex 标题排序、正文阅读宽度以及粗体/行内代码 Markdown 展示。

- app-server 架构迁移后的全量后端测试 53 项、前端组件测试 3 项、TypeScript 类型检查、sidecar 构建与 Tauri `--no-bundle` release 构建通过；协议专项测试覆盖真实 delta 和多轮 thread 续接。

- 后端单元、API 集成与故障恢复测试：50 项通过。
- 前端 TypeScript 类型检查通过。
- 前端组件测试通过。
- 统一发布门禁完整通过：布局检查、49 项后端测试、前端类型检查、3 项组件测试与补丁检查全部成功。
- 提交前严格审查已修复 READY 作业搁置、首次执行非原子认领、作业/审计信息泄露、前端状态陈旧、Unix 路径脱敏和旧发布产物假通过等问题；Ruff 静态检查全通过。
- 桌面 sidecar、前端 API/WebSocket 与 CSP 已统一迁移到 `127.0.0.1:8876`，避免本机既有 8765 服务冲突。
- 8876 release 运行验收通过：桌面进程启动、packaged sidecar、健康/诊断 API、数据库、数据目录及退出回收均正常。
- 8876 修复后的 NSIS 安装包已重新生成；MSI 在本机 WiX `light.exe` 链接阶段失败，旧 MSI 已删除并保持发布门禁失败，等待修复构建环境后重建。
- 安装验收发现并修复 release 桌面 EXE 缺少 Windows GUI 子系统标记导致的空终端窗口；NSIS 已重新构建、安装并确认使用 Windows GUI 子系统。
- 已备份用户数据库并清理 4 个历史演示/联调工作区及 10 个关联任务；安装版重启后工作区 API 返回空列表，首次使用空状态验收通过。
- 测试卡会显示当前测试命令和审批提示；未配置时改为明确空状态与“前往设置”，不再提供含义不清的直接运行按钮。
- 设置保存与连接检查已拆分：保存成功保持绿色确认和“已保存”状态，检查失败不会误报保存失败；修改字段后才恢复保存按钮。
- 设置窗口新增独立加载与失败重试状态：模型、配置和 Agent 健康检查完成前显示“检查中”并禁用保存，不再短暂显示空模型或“未连接”。
- 测试卡片的未配置入口已改为“配置测试命令”，会直接展开并定位测试执行器；模型配置与测试命令配置不再混淆，关闭设置后自动刷新卡片状态。
- Vite 与 Tauri release 构建通过。
- Release EXE：`apps/desktop/src-tauri/target/release/dualcode-workbench.exe`。
- Git 交接 E2E：隔离本地 `ecsMonitor` 副本从 `b7aa338` fast-forward 到 VPS/远端的 `4cd38a0`，同一仓库、同一提交且工作区干净。

## 当前正在验证

- VPS 首次克隆若留下非空但无效的目录，界面会提供“清理残留并重新克隆”。该操作单独审批，只允许作用于当前工作区自动派生的 VPS 项目目录，并在远端目录已是有效 Git 仓库时拒绝删除。
- VPS 仓库配置保存后切换为只读摘要和检测状态，不再持续显示编辑表单；仅在用户点击“修改配置”时恢复输入与保存操作。
- VPS 空仓库（已克隆且 origin 有效、但尚无 HEAD）现在识别为有效 Git 仓库，并显示“等待首次提交”，不再误报为克隆失败。
- VPS 仓库面板以当前实时检测结果为准；历史失败任务不再覆盖已恢复或已就绪的仓库状态。
- VPS 仓库提供无需审批的只读“刷新状态”；原先含义模糊的“VPS 获取”不再触发 `git fetch`。审批通过后若内存等待器丢失，持久化 READY 任务会由原子兜底执行器继续处理。
- 右侧仓库页移除手工提交说明、提交、拉取和推送控制条；Git 写操作由 Codex 根据任务结果提出，并继续经过应用审批和审计。
- 右侧仓库页进一步移除与“变更”页重复的未提交文件列表，以及低优先级的最近提交列表；保留仓库/VPS 状态、真实 Diff、运行日志、规则、交接、恢复和测试闭环。
- 新项目默认载入产品级治理基线：12 条规则覆盖需求澄清、架构决策、真实实现、安全审批、Git、测试、状态恢复、Codex 风险提醒和文档同步；7 项必备交付物覆盖设计、代码、验证、安全、风险和可重复发布。旧系统默认值会自动升级，用户自定义规则和交付物不会被覆盖。

- Codex 执行架构已从逐轮 `codex exec --json` 迁移到常驻 `codex app-server` JSON-RPC：支持真实回答 delta、推理/工具活动事件、多轮 thread 续接和 `turn/interrupt`。协议级自动化测试已覆盖新建会话与续接；本机 WindowsApps 执行别名受 ACL 拒绝，仍需配置当前用户可执行的独立 Codex CLI 路径完成真实联调。

- 真实 Codex CLI 在 Windows sidecar 下的仓库读取、多轮续接和流式输出。
- 工具活动摘要的最终视觉和完成状态。
- 真实 VPS Claude 多轮会话、图片附件和远端仓库上下文。

## 已知缺口

1. Git 外部副作用已有执行证据和保守对账，但真实远端服务异常、网络分区及凭据失效仍需人工故障注入验收。
2. 自动化测试已覆盖 Codex/Claude session 续接，但双 Agent 真实 CLI 第二轮和图片分析仍需完整人工验收。
3. NSIS 已重建但尚未在全新机器执行安装/升级/卸载；MSI 被本机 WiX `light.exe` 链接故障阻塞。
4. Monaco 已按变更页懒加载；编辑器独立资源仍较大，但不再影响初始应用包与启动路径。
5. 首次 Git 产品化基线尚未提交；远程仓库地址仍需配置。

## 下一步顺序

1. 完成真实 Codex 只读、多轮、停止和失败恢复验收。
2. 完成 VPS Claude 只读、多轮、图片和远端仓库验收。
3. 完成真实网络分区、远端拒绝和凭据失效的故障注入验收。
4. 扩展附件、审批、Agent、Diff、测试和审计的自动化端到端测试。
5. 清理演示数据并优化 Monaco 懒加载。
6. 构建 Windows 安装包并执行全新安装、升级和卸载验收。
7. 配置远程仓库并提交、推送产品化基线。

## 安全不变量

- 不读取或上传凭据文件。
- Claude 默认不获得本机写权限。
- 不把本地项目完整同步到 VPS。
- SSH 命令不拼接不可信输入。
- 模型调用、审批、Git 和文件变化必须留审计记录。
- 不自动合并或静默推送代码。

## 2026-07-14 Claude SSH 流式协议修复

- VPS Claude 与本地 Claude CLI 统一使用 `ClaudeStreamParser` 解析 `stream-json`。
- `system/init` 与限流元数据不再进入聊天正文；assistant 文本保持流式输出，工具事件进入活动摘要，非 JSON 诊断进入终端。
- 增加协议解析和 SSH 适配器回归测试，避免初始化 JSON 再次泄漏到消息区。

## 2026-07-14 消息区文档式样式修复

- 会话滚动容器仅允许纵向滚动；宽表格和长文本不再撑开整个消息区或产生底部横向滚动条。
- Codex 与 Claude 返回结果改为无背景、无边框、无左侧竖线的文档式正文，保留 Agent 身份和内容层级。
- 前端 66 项组件测试与 TypeScript 类型检查通过。
- 消息卡片与输入框共用 `--conversation-content-width` 内容轨道，宽度统一为 900px 并随窗口等比例收缩。
- Agent 正文按 Codex 文档式布局跨过头像占位，与输入框左右边界精确对齐；移除历史主题遗留的背景、边框和左侧强调线。

## 2026-07-14 Claude App 对齐 A1

- 对话列统一为 760px，回合间距使用 28px token；助手回复移除头像列并保持通栏纯文本，用户消息为右对齐 75% 最大宽度圆角气泡。
- 用户消息编辑改为气泡内 textarea，支持取消和“保存并重发”；保存会持久化编辑内容、复用原附件、触发该轮重发并记录审计事件。
- 前端 66 项、TypeScript、严格 ESLint、Ruff 与后端 113 项全量测试通过；Windows pytest 通过显式 `--basetemp` 避开系统临时目录的已知 ACL 故障。

## 2026-07-14 Claude App 对齐 A2

- 运行中思考改为「正在思考…」脉动标签与默认展开的次级色正文，去除斜体、左侧竖线和思考光标。
- 完成后自动收起为「已思考 X 秒」通栏行内条目，可通过右侧 chevron 展开回看，展开过渡为 150ms 并支持 reduced-motion。
- 验证通过：前端 67 项、TypeScript、严格 ESLint、A2 改动文件 Prettier、Vite 生产构建、Ruff、后端 113 项、
  Windows sidecar 与 Tauri release `--no-bundle` 构建。

## 2026-07-14 Claude App 对齐 A3

- 工具与命令活动移除「已处理 N 秒」外层折叠卡，改为按原顺序直接内联在消息流中的独立无边框圆角行。
- 每行显示工具类型图标、动作摘要和运行/完成/失败状态；展开后以等宽文本显示未截断的完整命令或输入。
- 失败摘要收敛到最后一条失败行，不再生成独立错误区；终端协议暂无调用 ID，因此通过「查看运行日志」安全降级到右侧日志页。
- 验证通过：前端 70 项、TypeScript、严格 ESLint、A3 改动文件 Prettier、Vite 生产构建、Ruff、后端 113 项、
  Windows sidecar 与 Tauri release `--no-bundle` 构建。

## 2026-07-14 Claude App 对齐 A4

- Codex/Claude 助手消息正文末尾新增常驻的低对比度图标操作行，提供复制 Markdown 原文与重试本轮；操作行进入正常文档流，
  不悬浮、不遮挡正文，复制成功后短暂显示对勾反馈。
- 助手消息的重试会关联其前方最近一条用户消息，继续复用既有后端重试、附件和审计语义，避免把助手消息 ID 传给只接受
  用户消息的接口。
- 验证通过：前端 70 项、TypeScript、严格 ESLint、A4 改动文件 Prettier、Ruff、后端 113 项全量测试、
  Vite 生产构建、Windows sidecar 与 Tauri release `--no-bundle` 构建。

## 2026-07-14 Claude App 对齐 A5

- Composer 保持附件、自动增高、IME、草稿、离线禁用和取消逻辑不变，调整为上方输入、下方操作的双行结构。
- 底栏左侧使用「+」附件入口和无边框 Agent 选择器，右侧使用 34px 圆形主按钮；空闲显示上箭头，运行中显示停止方块。
- 常驻快捷键文案已移除并进入 textarea tooltip，减少底栏噪声。
- 验证通过：前端 71 项、TypeScript、严格 ESLint、全量 Prettier、Ruff、后端 113 项全量测试、
  Vite 生产构建、Windows sidecar 与 Tauri release `--no-bundle` 构建。

## 2026-07-14 Claude App 对齐 A6

- 审批卡改为「允许 X？」标题、等宽完整操作内容和右对齐按钮组，并以 150ms 淡入进入消息流。
- 按钮层级调整为文字「拒绝」、编辑类操作专属次按钮「本次任务均允许」、实底主按钮「允许」。
- 审批 scope 映射和后端安全语义保持不变，没有扩大自动授权范围。
- 验证通过：前端 71 项、TypeScript、严格 ESLint、A6 改动文件 Prettier、Ruff、后端 113 项全量测试、
  Vite 生产构建、Windows sidecar 与 Tauri release `--no-bundle` 构建。

## 2026-07-14 Codex app-server 无进展恢复

- 确认 Codex CLI、登录和 app-server 基础协议可用；实际故障为单轮任务在进程仍存活时停止返回协议事件，旧实现会等待总超时 15 分钟，界面因此长期停留在“Codex 回复中”。
- 为 app-server 增加 60 秒无进展看门狗；任意协议事件都会刷新计时，连续无事件时终止失活进程并重置连接。历史会话由调度器自动降级为新会话重试，重试仍失败则向界面返回明确错误，不再无限转圈。
- 新增失活进程回归测试；Codex app-server 与 Agent 韧性测试共 19 项通过。

## 2026-07-14 消息正文排版密度对齐

- Codex 与 Claude 消息正文统一为 14px、400 字重和 1.65 行高，并优先使用 Windows 原生 `Segoe UI` / `Microsoft YaHei UI` 字体栈。
- Markdown 强调文字收敛为 600 字重，同时压缩段落、列表和列表项间距，降低原有大字号、粗字重和过度留白造成的视觉差异。
